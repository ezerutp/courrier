// Clase ClienteRepository desactivada temporalmente.
// Se reactivará si se implementan relaciones específicas con @ManyToOne.

/* package pe.transporte.courrier.repositories;

import java.util.Optional;

import org.springframework.data.jpa.repository.JpaRepository;

import pe.transporte.courrier.entities.Cliente;

public interface ClienteRepository extends JpaRepository<Cliente, Long>{
    Optional<Cliente> findByDni(String dni);

}
 */